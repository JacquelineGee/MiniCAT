"""
result_merge.py — 以路由为主键，合并所有分析结果，输出风险报告。（Step 10 实现）
"""
import logging
logger = logging.getLogger(__name__)

class ResultMerger:
    def merge(self, routes: list, events: list, states: dict, shares: dict) -> list:
        logger.warning("ResultMerger.merge: 尚未实现（Step 10）")
        return []
